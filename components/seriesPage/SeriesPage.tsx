import React from "react";
import {SafeAreaView, Text, View} from "react-native";
import {TextTypes} from "../../constants/TextTypes";


export const SeriesPage = ()=> {
    return (
        <SafeAreaView>
            <Text style={TextTypes.h4}>My Series</Text>

        </SafeAreaView>
    )
}
